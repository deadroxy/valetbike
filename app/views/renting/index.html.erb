<div class="page-section flexbox vertical stretch">
  <div class="section-inner flexbox vertical stretch">
    <div class="section-title" style="font-size: 25px;">
          <h1> Renting Page <h1>
    </div>  
    <div class="flexbox vertical stretch">
    <%= render(partial: "/stations/station_row", object: @station, as: :station) %>
    <% if user_signed_in? %>
        <button><%#= link_to 'Rent A bike', {controller: 'cards', action: 'index', station_id: @station[:id]}%>  
        <%= link_to 'Rent A bike', { action: 'create', station_id: @station[:id]}%>
        </button>
    <%else%>
        <p class="text" style="margin: 10px 350px"> Already a registered User? Login to rent a bike. Sign up otherwise <p>
    <%end%>
    </div>
	</div>
</div>
