<div class="bg-info text-white w-100 p-5 rounded-lg pageHeader">
  <h1 class="display-4">Return a Bike</h1>
  <p class="lead">Arrived at your destination? Return your bike at an available station!</p>
</div>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#" style="color:rgb(50, 110, 223);">ValetBike</a>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <%= link_to('Home', {:controller => 'stations', :action => 'index' }, :class => 'nav-link') %>
        <%= link_to('Membership Purchase', {:controller => 'payments', :action => 'index' }, :class => 'btn btn-payments nav-link') %>
        <%= link_to 'User Profile', current_user, :class => 'nav-link' %>
        <a class="nav-link active" href="#">Return a Bike</a>
      </div>
    </div>
  </div>
</nav>

<p id="notice" class="notice"><%= notice %></p>


<div class="container m-5">

<div class="choose-station text-center">
  <h2>Return a Bike!</h2>
  <p>Return a Bike at an available near-by station! Additional fees will be charged if the bike is not returned within the allotted times. One user can return one and only one bike at a time.</p>
  <br>
  <h4>Select a station to return a bike to:</h4>
  <script type="text/javascript">
    function myFunction() {
      document.getElementById("myDropdown").classList.toggle("show");
    }

    // Close the dropdown menu if the user clicks outside of it
    window.onclick = function(event) {
      if (!event.target.matches('.dropbtn')) {
        var dropdowns = document.getElementsByClassName("dropdown-content");
        var i;
        for (i = 0; i < dropdowns.length; i++) {
          var openDropdown = dropdowns[i];
          if (openDropdown.classList.contains('show')) {
            openDropdown.classList.remove('show');
          }
        }
      }
    }
  </script>

  <div class="dropdown">
  <%= form_tag(returns_update_path, method:'PATCH') do %>
  <select name="stationId" id="stationId" class="text-center">
    <% @stations.each do |s| %>
      <option value=<%= s.identifier %>><%= s.name %></option>
    <% end %>
  </select>
  </div>

  <br><br><br>
  <%= submit_tag 'Return Now', :class => "button2" %>
  
  <%
=begin%>
 <html>
    <head>
        <title>Display Alert Message on Button Click Event.</title>
        <script type="text/javascript">
            function showMessage() {
                alert("You have successfully returned your bike!");
            }
        </script>
    </head>
    <body>
        <center>
            <br><br><br>
            <input type="button" id="btnShowMsg" value="Return Bike" onClick="showMessage()" class="button2" />
        </center>
    </body>
</html> 
<%
=end%>
  <% end %>


</div>