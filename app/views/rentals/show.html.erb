<div class="stations show">
<div class="page-section flexbox vertical stretch">
  <div class="section-inner flexbox vertical stretch">
    <div class="section-title">
    <% if !current_user.ongoing_rental? %>
      You do not have an ongoing rental.
    <% else %>
      Renting bike <%= @rental.bike.identifier %><br>
    <% end %>
    <% if current_user.ongoing_rental? %>
        This rental started at <%= @rental.start_time.localtime.strftime("%I:%M %P ") %> 
        <% if @rental.start_time.today? %> 
        today.
        <% else %>
        on <%= @rental.start_time.strftime("%B %-d, %Y") %>.
        <% end %>
        <br>
        You will be charged 10 cents for every minute after <%= (@rental.start_time.localtime + @rental.time_limit.minute).strftime("%I:%M %P ") %><br>
    <% end %>
    </div>
	</div>
  </div>