<div class="bg-info text-white w-100 p-5 rounded-lg pageHeader">
  <h1 class="display-4">User Profile</h1>
  <p class="lead">Welcome to ValetBike, <%= @user.first_name %>!</p>
</div>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#" style="color:rgb(50, 110, 223);">ValetBike</a>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <%= link_to('Home', {:controller => 'stations', :action => 'index' }, :class => 'nav-link') %>
      <%#  <%= link_to("Log In", new_session_path, :class => 'nav-link')%>
    <%#    <%= link_to("Sign Up", new_user_path, :class => 'action new nav-link') %>
    <%#    <%= link_to('User List', {:controller => 'users', :action => 'index' }, :class => 'nav-link') %>
        <%= link_to('Membership Purchase', {:controller => 'payments', :action => 'index' }, :class => 'btn btn-payments nav-link') %>
        <a class="nav-link active" href="#">User Profile</a>
        <%= link_to("Return a Bike", "/return_bike", :class => 'nav-link') %>
      </div>
    </div>
  </div>
</nav>

<p id="notice"><%= notice %></p>

<table class="table table-striped table-hover w-75 m-5">
  <thead>
    <tr class="table-dark">
      <th>Email</th>
      <th>First name</th>
      <th>Last name</th>
      <th>Current Membership</th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><%= @user.email %></td>
      <td><%= @user.first_name %></td>
      <td><%= @user.last_name %></td>
      <td><%= @user.membership %></td>
    </tr>
  </tbody>
</table>


<%= link_to 'Edit', edit_user_path(@user), :class => "btn btn-primary m-3" %>
<%= link_to 'Log Out', session_path, method: 'DELETE', :class => "btn btn-warning" %>
