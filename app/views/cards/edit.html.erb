<!DOCTYPE html>
<html>
  <head>
    <style>
      .edit_card_form { 
        position: fixed;
        top: 50%;
        left: 50%;
        width: 20em;
        height: 18em;
        transform: translate(-50%, -50%);
      }

      .form_button {
        padding: 15px 32px;
        text-align: center;
        display: inline-block;
        font-size: 16px;
        margin: 4px 2px;
        cursor: pointer;
        color: white;
        background-color: #8C1627;
        border-radius: 10px;
        width: 25%
      }
    </style>
  </head>
  <body>
    <div class = "edit_card_form", align = "center", height = "150px">
      <h2> Edit Card Information </h2>

      <%= form_for(@card) do |f| %>
        <% # url: card_path(@card), method: 'patch' %>
      
        <%= render(partial: 'form', locals: {f: f}) %>

        <div class = "form_button">
          <%= f.submit("Edit Card")%>
        </div>

        <% end %>
    </div>
  </body>
</html>

