<div class="record-row <%= cycle "odd", "even", name: "row-cycler" %> flexbox vertical stretch">
  <%= station.identifier %>: <%= station.name %> (<%= station.address %>) [<%= station.docked_bikes.count %> docked bikes]
  <% if user_signed_in? %>
  <%= link_to("Rent", station_path(station), class: 'action show') %>
  <% end %>
</div>