<div class="record-row <%= cycle "odd", "even", name: "row-cycler" %> flexbox horizontal justify-space-between">
  <div><p><%= station.identifier %>: <%= station.name %> (<%= station.address %>)</p></div>
  <div><h4 style="color:#0CCAA8;text-align:center;">  <% station.docked_bikes.count %></h4> </div>
  <%=link_to('select station', station_path(station.identifier)) %>
</div>