 <% bikes = Bike.where(current_station_identifier: @station.identifier ) %>
 Station identifer: <%= @station.identifier %> <br>
 
 Station name: <%= @station.name %> <br>
 
 <% if (bikes.length > 0)%>

    Number of scooters avilable: <%= bikes.length%> <br>
    Number of docks available: <%= @station.dock_count - bikes.length%> <br>

    <% bikes = Bike.where(current_station_identifier: @station.identifier ) %>
        <%bikes.each do |bike| %>
          <br>
        <%= bike.identifier%>
        <%= link_to("click here to rent this bike", new_rental_path(bike: bike.id, station: @station.id)) %>
        <% end%>
    <% else %>
    all out of scooters here! please leave  
    <% end  %>

    <br>
    <br>

    <%= link_to("return to stations list", "/index")%>
    <%= link_to("view stations map", "map")%>





<br><br><br>
 note: the rails id of this station is <%= @station.id %>


      