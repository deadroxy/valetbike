<div class="page-section flexbox vertical stretch">
  <div class="section-inner flexbox vertical stretch">
    <div class="section-title">
      <h1>🚲ValetBike🚲</h1>
    </div>  
    <div class="flexbox vertical stretch">
      <% if @stations.present? %>
        <%#= render(partial: "stations/row", collection: @stations, as: :station) %>

        <%# https://medium.com/swlh/using-rails-and-html-to-view-sort-and-search-tables-fbf8a0543558%>
        <table>
          <tr>
            <th><%="Station Name"%> </th>
            <th><%="Address"%> </th>
          </tr>

        <% @stations.each do |station| %>
          <tr>
            <td><%=station.name%></td>
            <td><%=station.address %></td><td>
          </tr>
          <% end %>
          </table>

      <% else %>
      <div class="empty">
        No stations found.
      </div>
      <% end %>
    </div>
	</div>
</div>
