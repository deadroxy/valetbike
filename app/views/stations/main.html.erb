<div class="page-section flexbox vertical stretch">
  <div class="section-inner flexbox vertical stretch">

    <div class="section-title flexbox horizontal center">
      <h2> Welcome to bikesia! </h2>
      <% if Current.user.try(:admin?) %>
        <a class="toggle" href="<%= add_stations_path %>">Add Stations</a> 
        <a class="toggle" href="<%= add_bikes_path %>">Add Bikes</a> 
      <% end %>
      

      <button class="unlock-button"><%=link_to('View Bike Fleet', bikes_path )%></button>
      <button class="unlock-button"><%=link_to('Homepage', root_path)%></button>
    </div>  

    <div class="flexbox vertical stretch">
      <% if @stations.present? %>
       <%= render(partial: "stations/row", collection: @stations, as: :station) %>
      
      <% else %>
      <div class="empty">
        No stations found.
      </div>
      <% end %>

      <div class="rights-text">
      <br></br>
      2023 Version 01 CSC 223-01
      </div>  

    </div>
  </div>
</div>